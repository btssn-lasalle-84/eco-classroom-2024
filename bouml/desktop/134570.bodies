class BaseDeDonnees
!!!134570.cpp!!!	getInstance() : BaseDeDonnees
    if(bdd == nullptr)
        bdd = new BaseDeDonnees();
    nbAcces++;
#ifdef DEBUG_BDD
    qDebug() << Q_FUNC_INFO << "nbAcces" << nbAcces;
#endif

    return bdd;
!!!134698.cpp!!!	detruireInstance() : void
    if(bdd != nullptr)
    {
        nbAcces--;
#ifdef DEBUG_BDD
        qDebug() << Q_FUNC_INFO << "nbAcces" << nbAcces;
#endif
        if(nbAcces == 0)
            delete bdd;
    }
!!!134826.cpp!!!	connecter(in nomBase : QString = DATABASENAME, in username : QString = USERNAME, in password : QString = PASSWORD, in serveur : QString = HOSTNAME) : bool
    if(!db.isOpen())
    {
        db.setHostName(serveur);
        db.setUserName(username);
        db.setPassword(password);
        db.setDatabaseName(nomBase);

#ifdef DEBUG_BDD
        qDebug() << Q_FUNC_INFO << "HostName" << db.hostName() << "UserName" << db.userName()
                 << "DatabaseName" << db.databaseName();
#endif
        if(db.open())
        {
            return true;
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : impossible de se connecter "
                                          "à la base de données !");
            QMessageBox::critical(
              nullptr,
              QString::fromUtf8("BaseDeDonnees"),
              QString::fromUtf8("Impossible de se connecter à la base de données !"));
            return false;
        }
    }
    else
        return true;
!!!134954.cpp!!!	estConnectee() : bool
    return db.isOpen();
!!!135082.cpp!!!	executer(in requete : QString) : bool
    QSqlQuery r;
    bool      retour;

    if(db.isOpen())
    {
        if(requete.contains("UPDATE") || requete.contains("INSERT") || requete.contains("DELETE"))
        {
            retour = r.exec(requete);
#ifdef DEBUG_BDD
            qDebug() << Q_FUNC_INFO << QString::fromUtf8("requête : %1").arg(requete);
#endif
            if(retour)
            {
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !").arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135210.cpp!!!	recuperer(in requete : QString, inout donnees : QString) : bool
    QSqlQuery r;
    bool      retour;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
#ifdef DEBUG_BDD
            qDebug() << Q_FUNC_INFO << QString::fromUtf8("requête : %1").arg(requete);
#endif
            if(retour)
            {
                // on se positionne sur l'enregistrement
                r.first();

                // on vérifie l'état de l'enregistrement retourné
                if(!r.isValid())
                {
#ifdef DEBUG_BDD
                    qDebug() << Q_FUNC_INFO << QString::fromUtf8("Erreur : résultat non valide !");
#endif
                    return false;
                }

                // on récupère sous forme de QString la valeur du champ
                if(r.isNull(0))
                {
#ifdef DEBUG_BDD
                    qDebug() << Q_FUNC_INFO << QString::fromUtf8("Erreur : aucun résultat !");
#endif
                    return false;
                }
                donnees = r.value(0).toString();
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !").arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135338.cpp!!!	recuperer(in requete : QString, inout donnees : QStringList) : bool
    QSqlQuery r;
    bool      retour;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
#ifdef DEBUG_BDD
            qDebug() << Q_FUNC_INFO << QString::fromUtf8("requête : %1").arg(requete);
#endif
            if(retour)
            {
                // on se positionne sur l'enregistrement
                r.first();

                // on vérifie l'état de l'enregistrement retourné
                if(!r.isValid())
                {
#ifdef DEBUG_BDD
                    qDebug() << Q_FUNC_INFO << QString::fromUtf8("Erreur : résultat non valide !");
#endif
                    return false;
                }

                // on récupère sous forme de QString la valeur de tous les
                // champs sélectionnés et on les stocke dans une liste de
                // QString
                for(int i = 0; i < r.record().count(); i++)
                    if(!r.isNull(i))
                        donnees << r.value(i).toString();
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !").arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135466.cpp!!!	recuperer(in requete : QString, inout donnees : QVector<QString>) : bool
    QSqlQuery r;
    bool      retour;
    QString   data;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
#ifdef DEBUG_BDD
            qDebug() << Q_FUNC_INFO << QString::fromUtf8("requête : %1").arg(requete);
#endif
            if(retour)
            {
                // pour chaque enregistrement
                while(r.next())
                {
                    // on récupère sous forme de QString la valeur du champs
                    // sélectionné
                    data = r.value(0).toString();

                    // on stocke l'enregistrement dans le QVector
                    donnees.push_back(data);
                }
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !").arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135594.cpp!!!	recuperer(in requete : QString, inout donnees : QVector<QStringList>) : bool
    QSqlQuery   r;
    bool        retour;
    QStringList data;

    if(db.isOpen())
    {
        if(requete.contains("SELECT"))
        {
            retour = r.exec(requete);
#ifdef DEBUG_BDD
            qDebug() << Q_FUNC_INFO << QString::fromUtf8("requête : %1").arg(requete);
#endif
            if(retour)
            {
                // pour chaque enregistrement
                while(r.next())
                {
                    // on récupère sous forme de QString la valeur de tous les
                    // champs sélectionnés et on les stocke dans une liste de
                    // QString
                    for(int i = 0; i < r.record().count(); i++)
                        data << r.value(i).toString();

                    // on stocke l'enregistrement dans le QVector
                    donnees.push_back(data);

                    // on efface la liste de QString pour le prochain
                    // enregistrement
                    data.clear();
                }
                return true;
            }
            else
            {
                qDebug() << Q_FUNC_INFO
                         << QString::fromUtf8("Erreur : %1 pour la requête %2")
                              .arg(r.lastError().text())
                              .arg(requete);
                return false;
            }
        }
        else
        {
            qDebug() << Q_FUNC_INFO
                     << QString::fromUtf8("Erreur : requête %1 non autorisée !").arg(requete);
            return false;
        }
    }
    else
        return false;
!!!135722.cpp!!!	BaseDeDonnees()
#ifdef DEBUG_BDD
    qDebug() << Q_FUNC_INFO;
#endif
    db = QSqlDatabase::addDatabase("QMYSQL");
!!!135850.cpp!!!	~BaseDeDonnees()
#ifdef DEBUG_BDD
    qDebug() << Q_FUNC_INFO;
#endif
